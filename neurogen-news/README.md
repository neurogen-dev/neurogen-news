# Neurogen.News

🧠 Медиа о нейросетях и AI — гайды, обзоры, новости и инструменты для работы с искусственным интеллектом.

## Технологии

### Frontend
- **Vue 3** + TypeScript
- **Vite** — сборка и dev-сервер
- **Tailwind CSS** — стилизация
- **Pinia** — state management
- **TipTap** — редактор статей
- **Vue Router** — маршрутизация

### Backend
- **Go 1.23+** — основной язык
- **Fiber v2** — HTTP фреймворк
- **PostgreSQL 17** — основная БД
- **Redis 7.4+** — кэширование и сессии
- **Meilisearch** — полнотекстовый поиск

### Инфраструктура
- **Docker** — контейнеризация
- **Cloudflare** — CDN и защита

## Быстрый старт

### Требования
- Go 1.23+
- Node.js 20+
- Docker и Docker Compose
- Make (опционально)

### Запуск через Docker

```bash
# Клонировать репозиторий
git clone https://github.com/neurogen-news/neurogen-news.git
cd neurogen-news

# Скопировать и настроить env
cp .env.example .env

# Запустить все сервисы
docker compose up -d

# Открыть http://localhost:8080
```

### Запуск для разработки

```bash
# Запустить базы данных
docker compose up -d postgres redis meilisearch

# Установить зависимости фронтенда
cd frontend && npm ci

# Запустить фронтенд (в отдельном терминале)
npm run dev

# Запустить бэкенд (в отдельном терминале)
cd backend && go run ./cmd/server
```

## Структура проекта

```
neurogen-news/
├── frontend/                # Vue 3 SPA
│   ├── src/
│   │   ├── components/     # UI компоненты
│   │   ├── pages/          # Страницы
│   │   ├── stores/         # Pinia stores
│   │   ├── composables/    # Vue composables
│   │   ├── api/            # API клиент
│   │   ├── types/          # TypeScript типы
│   │   └── utils/          # Утилиты
│   └── ...
├── backend/                 # Go API
│   ├── cmd/server/         # Точка входа
│   ├── internal/
│   │   ├── config/         # Конфигурация
│   │   ├── handler/        # HTTP хэндлеры
│   │   ├── service/        # Бизнес-логика
│   │   ├── repository/     # Работа с БД
│   │   ├── model/          # Модели данных
│   │   └── middleware/     # Middleware
│   └── migrations/         # SQL миграции
├── docker-compose.yml      # Production compose
├── Dockerfile              # Production образ
└── Makefile               # Команды сборки
```

## API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` — Регистрация
- `POST /api/v1/auth/login` — Вход
- `POST /api/v1/auth/refresh` — Обновление токена
- `POST /api/v1/auth/logout` — Выход

### Статьи
- `GET /api/v1/articles` — Список статей
- `GET /api/v1/articles/:id` — Получить статью
- `POST /api/v1/articles` — Создать статью
- `PUT /api/v1/articles/:id` — Обновить статью
- `DELETE /api/v1/articles/:id` — Удалить статью

### Пользователи
- `GET /api/v1/users/me` — Текущий пользователь
- `GET /api/v1/users/:username` — Профиль пользователя
- `PUT /api/v1/users/me` — Обновить профиль

### Комментарии
- `GET /api/v1/comments/article/:articleId` — Комментарии к статье
- `POST /api/v1/comments` — Создать комментарий
- `PUT /api/v1/comments/:id` — Обновить комментарий
- `DELETE /api/v1/comments/:id` — Удалить комментарий

## Команды Make

```bash
make help           # Показать все команды
make dev            # Запустить разработку
make build          # Собрать для production
make test           # Запустить тесты
make lint           # Линтинг кода
make docker-up      # Запустить Docker
make docker-down    # Остановить Docker
make migrate        # Применить миграции
```

## Переменные окружения

См. `.env.example` для полного списка.

Ключевые переменные:
- `DATABASE_URL` — PostgreSQL connection string
- `REDIS_URL` — Redis connection string
- `JWT_SECRET` — Секрет для JWT токенов
- `CORS_ORIGINS` — Разрешённые origins для CORS

## Лицензия

MIT

